import React from 'react';
import { 
  Background, 
  LoadingSpinner,
} from './style';

const Loading = () => {
  return (
    <Background>
      <LoadingSpinner />
    </Background>
  );
};

export default Loading;
